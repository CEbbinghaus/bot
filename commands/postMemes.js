// if (m.author.id != Bot.user.id && m.channel.type == "text" && m.guild.id != "407841768059437056") return;

  // if (Timer) {
  //   clearTimeout(Timer);
  // }
  // Timer = setTimeout(() => {
  //   fetch("http://www.reddit.com/r/dankmemes.json").then(async r => {
  //     let j = await r.json();
  //     console.log(j.data.children[Math.random() * (j.data.children.length - 2) | 0 + 2].data.url);
  //   })
  // }, 1e4)